#include "main.h"
/**
 * print_last_digit - prints the last digit
 *
 * @n: number to print
 * Return: last digit of integer
 */
int print_last_digit(int n)
{
	int f;

	if (n >= 10)
	{
		f = n % 10;
		return (f);
	}
return (n);
}
