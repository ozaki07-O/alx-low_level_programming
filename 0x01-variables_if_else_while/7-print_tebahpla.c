#include <stdio.h>
/**
 * main - prints the alphabets reversed in lowercase
 *
 * Return: Always 0
 */
int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
